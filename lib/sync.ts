// Deprecated: Offline-first synchronization engine has been decommissioned.
// The application now queries and mutates the online database directly.
export const syncData = async (silent = false) => {
  // No-op
};
